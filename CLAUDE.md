# Claude.md

Before making changes or giving implementation guidance, always look for and follow any `AGENTS.md` files in the repository.

Apply them like this:

- Read the root `AGENTS.md` first.
- If working in a subdirectory, also look for a closer `AGENTS.md` in that subtree.
- If they conflict, prefer the more specific `AGENTS.md`.
- Re-check relevant `AGENTS.md` files when switching folders or editing files in a different area of the repo.
