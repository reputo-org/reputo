# PostgreSQL Configuration for the API application database.
# Loaded by docker/compose/dev.yml into the `postgres` service.
POSTGRES_USER=reputo_app
POSTGRES_PASSWORD=reputo_app
POSTGRES_DB=reputo_app
