# MongoDB Environment Configuration Example
# Copy this file to mongodb.env and update with your actual values

# MongoDB Root Credentials.
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=reputo-dev

# Initial Database (optional)
MONGO_INITDB_DATABASE=reputo_dev
