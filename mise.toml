[tools]
node = "24.15.0"
pnpm = "11.13.0"

[settings]
auto_install = false

[tasks.setup]
description = "First-time setup: install tools, copy .env, install deps"
run = [
  "mise install",
  "test -f .env || cp .env.example .env",
  "pnpm install",
]
